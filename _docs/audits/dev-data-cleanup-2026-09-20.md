# dev のデータ掃除（2026-09-20）— 今の規則では作れない出荷書を消した

対象は **dev の `ckk` データベースだけ**（本番は触っていない）。スキーマは動かして
いない — 消したのは行 2 本。

## 何を消したか

| 消したもの | 状態 | 作られた日 |
|---|---|---|
| `DOR-202609-00003`（出荷明細 2 行つき） | 確定 | 2026-09-04 16:39 JST |
| `DRN-202609-00002`（納品明細 2 行つき） | 発行済 | 同上（確定と同時に自動作成） |

`DOR-202609-00002` と**同じ注文明細・同じ本数**を載せた二重の出荷書だった。
受注はどちらの明細も 2 本なのに、2 通合わせて 4 本が割り当てられていた。

## なぜ作れてしまったのか

残数の判定が「**出荷済の出荷書だけ**」を数えていた時期に作られたため。
`a64e989e`（2026-09-04 20:28 JST、「残数ガード: SHIPPED だけでなく下書き・確定の
出荷書も消費として数える」）で今の規則になったが、この出荷書はその **約 4 時間前**
に作られている。当時 `DOR-202609-00002` は確定のまま未出荷だったので、
古い規則では「残り 2 本」に見えていた。

いまの規則で同じものを作ろうとすると、作成の時点で断られる
（割当 4 > 受注 2、かつ過不足の許可が無い — 指示書 #5 は
`allow_quantity_variance = false`、`ORD-202608-00007-02` には指示書が無い）。

## なぜ消す以外に道が無かったか

- **出荷できない**。出荷時の過出荷ガードが累計 4 > 受注 2 で止める（正しい）。
- **キャンセルできない**。`deleteDeliveryOrder` は下書きしか消さない。
- つまり画面からはどちらにも動かせない書類だった。**確定済みの出荷書を
  取り消す道が無いこと自体は残った課題**で、直すなら「確定の承認と同じ重さの
  取り消し」をどう置くかの設計が要る。

消す前の行の内容は、この掃除を実行したセッションのログに JSON で残してある。
子（承認依頼・メモ・添付・追加料金）は 0 件、請求書からの参照も無かった。
監査ログ（`audit_logs`）は規約どおり消していない。

## 直さずに残したもの（今の規則に反していない / 反しているが手で作ると嘘になる）

- **マイナス在庫 2 行**（`PRD-202608-0001` / `-0002` が各 −2）。
  在庫が無いまま出荷した記録で、#907 以降は**正しい状態**（在庫一覧で赤く出る）。
  戻すなら棚卸か手動入出庫で。
- **納品書の無い出荷済 2 通**（`DOR-202608-00001` / `DOR-202609-00002`）。
  納品書の自動作成より前に確定されたもの。いま手で作ると、お客様へ出していない
  書類に番号が付く — 作らない。
