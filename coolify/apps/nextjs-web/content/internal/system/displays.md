---
title: "ディスプレイ管理 — 操作マニュアル"
description: "操作コード SY0I。現場のテレビ（管理ディスプレイ）に何を映すかを決めるアプリです。画面の登録・表示内容の割り当て・一時停止や失効を、現地に行かずに操作できます。"
---

操作コード **SY0I**。現場のテレビに何を映すかを決めるアプリです。

> このアプリは現在 **検証環境（dev）のみ** で公開されています。本番公開までに
> 画面や手順が変わる場合があります。

> このアプリの利用には **共有端末管理の権限** が必要です。**登録**と**失効**は
> 特権操作で、ほかの人の承認を経た期間だけ実行できます（特権アクセス SY0G）。
> すべての操作は監査ログに記録されます。

## このアプリでできること

- 現場のテレビを一覧で見られます（どこに何台あって、いま点いているか）
- それぞれのテレビに映すものを決められます
- 映すものをあとから切り替えられます（テレビには触りません）
- 使わなくなった画面を止めたり、取り消したりできます

## 用語（このページで出てくる言葉）

- **ディスプレイ** … 現場に置いたテレビ 1 枚のことです。
- **表示内容** … 「何を映すか」の設定です。1 つの表示内容を複数のテレビで
  共有できます。
- **登録コード** … テレビの画面に出る 12 文字です。「この画面はどれか」を
  伝えるために使います。

## はじめる前に

- テレビ側の準備（Raspberry Pi の設置）は
  [ディスプレイの設置（Raspberry Pi）](/admin-manual/ja/system/display-setup)
  を先に行ってください。
- 映すものを決めるには、先に **表示内容** を 1 つ以上つくっておくと楽です
  （登録のときに選べます）。

## 画面の状態

| 状態 | 意味 |
|---|---|
| オンライン | いま通信できています（直近 5 分以内） |
| オフライン | 電源が入っていないか、ネットワークが切れています |
| 有効 | 通常の状態です |
| 一時停止 | こちらから止めています。テレビには登録画面が出ます |
| 失効 | 登録を取り消しました。もう一度使うには登録し直します |

## ディスプレイを登録する

1. テレビの画面に QR コードと 12 文字の登録コードが出ていることを確認します。
2. スマートフォンのカメラで QR コードを読み取ります。
   （パソコンから行う場合は「ディスプレイを登録」を押します）
3. 「登録コード」に 12 文字が入っていることを確認します。
   入っていなければ手で入力します。
4. 「ディスプレイの名前」を入れます。現場の人が呼ぶ名前にします。
5. 「設置場所」「拠点」「表示内容」を選びます。
6. 「登録する」を押します。

テレビの表示が数秒で切り替わります。

> ⚠️ 登録コードは **10 分**で使えなくなります。過ぎたときは、テレビの画面の
> 「新しいコードを発行」を押して新しいコードを出してください。

## 表示内容を切り替える

1. 一覧から対象のディスプレイを押します。
2. 「設定」タブの「表示内容」を選び直します。
3. 「保存」を押します。

テレビの表示がその場で切り替わります。**現地に行く必要はありません。**

## 表示内容をつくる

一覧の「表示内容」から、映すものを登録・編集できます。

| 種別 | 何を映すか |
|---|---|
| アプリの画面 | 生産状況などの、このシステムの画面 |
| 集計ダッシュボード | 集計ツール（Metabase）のダッシュボード |
| 外部ページ | 指定した URL のページ |
| 画像 | 画像 1 枚（お知らせ・注意喚起など） |

「再読込の間隔（秒）」は、映しているものを自動で読み直す間隔です。
`0` にすると自動では読み直さず、こちらで変更したときだけ切り替わります。

> ⚠️ 使用中の表示内容は削除できません。先に、使っているディスプレイの
> 割り当てを変えてください。

### 集計ダッシュボードを映すとき

集計ツール側でも設定が必要です。ダッシュボードごとに「埋め込み」を有効にし、
**拠点やラインの絞り込みを「ロック」に設定してください。** ロックしないと、
現場の画面から他の拠点のデータが見えてしまう状態になります。
設定方法は情報システム担当にご確認ください。

## 止める・取り消す

| やりたいこと | 操作 | あとで戻せるか |
|---|---|---|
| 一時的に止める | 詳細画面の「一時停止」 | 「再開」で戻せます |
| 使うのをやめる | 詳細画面の「失効」 | 登録し直しが必要です |
| 記録ごと消す | 失効させてから「削除」 | 戻せません |

**失効させると、そのテレビは次の読み込みで登録画面に戻ります。** 別の場所へ
持って行って使い直せます。故障や紛失のときも、この操作でその画面からは
何も見えなくなります。

## よくある質問

**Q. 一覧でずっとオフラインのままです**
A. 現地でテレビと Raspberry Pi の電源、LAN ケーブルを確認してください。
通信が戻れば自動でオンラインに戻ります。

**Q. 「表示内容 未割当」と出ています**
A. そのディスプレイにまだ映すものを決めていません。詳細画面で
「表示内容」を選んで「保存」してください。

**Q. 登録しようとすると「この登録コードは既に使われています」と出ます**
A. そのコードでは既に別の登録が済んでいます。テレビの画面に出ている
新しいコードを読み取ってください。

**Q. 同じ内容を複数のテレビに映せますか**
A. できます。同じ「表示内容」を複数のディスプレイに割り当ててください。
その表示内容を変えると、割り当てた全部のテレビが一度に切り替わります。
