---
title: "用户管理 — 操作手册"
description: "操作代码 SY01。用户名录应用：查看系统中注册的全部用户，以及每个用户的角色分配、实际生效的权限与所属工厂。"
---
操作代码 **SY01**。用户名录应用：查看系统中注册的全部用户，以及每个用户的角色分配、实际生效的权限与所属工厂。

> 本应用需要 **系统权限**。在此界面上不能创建、编辑、删除用户或变更角色。唯一的例外是 **所属工厂**：只有系统管理员（`system:ADMIN`）可以编辑（变更会记录到审计日志）。

## 本应用能做什么

- 以列表方式浏览全部用户，支持搜索和筛选。
- 打开用户详情，查看其角色分配（含历史记录）。
- 查看该用户的 **实际生效权限**（权限代码 × 操作 × 范围）——即用户当前实际能做的事。
- 查看 **所属工厂**（PLANT / REGION 范围权限的适用对象）；系统管理员可以编辑。

## 打开方式

- 主页（系统）→ **ユーザー管理（用户管理）**，或在搜索框输入 `SY01`。

## 列表与搜索

列表显示以下各列。

- **用户名** … 登录用 ID（等宽字体）。
- **显示名** … 界面上显示的姓名。
- **邮箱** … 注册的邮箱地址（无则显示"—"）。
- **类别** … **系统** / **员工** / **访客** 徽章。
- **角色** … 当前生效角色的徽章（可有多个）。
- **状态** … **有效** / **无效**。
- **最后登录** … 最后一次登录的时间。

搜索框可同时匹配用户名、显示名、邮箱和角色名。可用 **类别** 和 **状态** 下拉框筛选，点 **重置** 清除全部条件。点击行进入详情页。

## 用户详情

详情页由四个区块组成。

- **概要** … 用户名、类别、邮箱、登录方式（**密码 + SSO** 或 **仅 SSO**）、最后登录、员工 ID。
- **角色分配** … 分配历史列表：角色名、rolename（系统内部名）、状态（有效/无效）、分配日期、解除日期。已解除的历史分配也会显示。
- **所属工厂** … 「工厂」「地域」范围的权限所适用的工厂。系统管理员（`system:ADMIN`）会看到选择栏与保存按钮，只有这一块可以编辑（`updateUserPlants` — 变更会记录到审计日志）。其他用户只显示徽章。
- **实际生效权限** … 排列经由有效角色获得的 grant 的"该用户当前能做什么"清单。按权限代码（如 `quote`、`master`、`system`）显示操作（READ / CREATE / UPDATE 等）和范围。

## 如何理解实际生效权限

- 权限属于角色，一个用户可以拥有多个角色。
- 列表显示 **以 grant（角色 × 权限 × 操作）为单位的全部行**。同一权限代码・操作的行可能来自多个角色而重复出现，这不是异常（实际能否访问由应用侧按全部行的**并集**解决 — 例：同时有 PLANT 和 ALL 时，相当于 ALL）。
- 范围指该操作的覆盖面（ALL = 全部、PLANT = 所属工厂、OWN = 本人数据 等）。PLANT / REGION 判定所依据的工厂在「所属工厂」区块中设置。

## 常见问题

- **想添加或编辑用户** … 此界面上无法操作。用户信息通过与公司认证目录（AD）同步等方式管理，请联系系统管理员。
- **想变更角色** … 此界面仅供查看。角色分配的变更由系统管理员进行。
- **权限与预期不符** … 请查看详情页的"实际生效权限"。多个角色的 grant 行会全部并列，实际访问是它们的并集。有「工厂」「地域」范围的权限却看不到数据时，也请确认「所属工厂」是否为空。
- **想更改他人的语言或日期时间显示** … 无法从本画面更改。语言、日期/时间格式与时区属于**仅本人可更改**的个人设置，由本人从头像菜单的"显示设置"（网页端）或 kiosk 启动器（平板）切换。语言设置在网页端与 kiosk 之间共用。
- 想查看谁在何时改了什么，请使用[操作历史](/admin-manual/zh/system/activity-log)。
- 应用的显示开关请参见[应用管理](/admin-manual/zh/system/app-management)。
