---
title: "未处理指示单 — 操作手册"
description: "在一个画面上确认「指示单还没配够的订单明细」和「尚未完成的指示单」的工作队列。"
---
这是把「还没有做指示单的订单」和「做了但还没结束的指示单」放在一起确认的 **未処理指示書**（未处理指示单）应用。操作码是 `PD05`。

## 本应用能做什么

- 可以用一览的方式查看已确定的订单明细中、指示单还没安排够的项目。
- 可以在缺少的数量已经填好的状态下，直接进入[制造指示书](/manual/zh/operations/production/work-order/user)的新建画面。
- 可以确认已经做好、但还没有完成的指示单（草稿、待审批、已审批、进行中）。
- 按交货期从近到远决定「今天先给哪个订单做指示单」时很方便。

## 本页出现的词语

- **注文明細**（订单明细）… 订单请书中的一行。「这个产品做几根」的接单单位。
- **指示書**（制造指示书）… 「这个产品做几根」的制造指示。
- **手配**（安排）… 给订单明细制作指示单。一条明细也可以拆成多张指示单。
- **未手配**（未安排）… 接单数量中还没有变成指示单的数量。计算方式是 **未安排 = 接单数 − 已安排数**。

## 如何打开

按主页「生産」（生产）中的 **未処理指示書**（未处理指示单）。或者在画面上方的搜索栏输入 `PD05`。查看需要指示单的权限。

## 画面说明

画面上有两个标签页。两边都是只显示「还没处理完的东西」的工作队列。

- **未手配**（未安排）… 指示单还没配够的订单明细。件数用橙色显示。
- **進行中**（进行中）… 已创建、尚未完成或取消的指示单。

可以用上方的搜索栏和「**状態**」（状态）筛选 — 在进行中标签页还可以用「**種別**」（类别）。想清除条件时按「**リセット**」（重置）。

### 未安排标签页

已确定的订单明细中，指示单数量还没达到接单数量的项目，按 **交货期从早到晚** 排列。

- **注文明細番号**（订单明细编号）… 点击后打开[订单明细](/manual/zh/operations/sales/order-line/user)的详情。
- **顧客 / 製品**（客户 / 产品）… 是谁下的订单、订的哪种产品。
- **受注数**（接单数）… 订购的数量。
- **手配済**（已安排）… 已经分配给指示单的数量合计。已取消的指示单不计入。另外，已完成的指示单如果因不良而数量不足，缺少的部分会回到未安排。
- **未手配**（未安排）… 接单数 − 已安排数。用橙色徽章显示。
- **在庫引当**（库存预留）… 已从产品库存预留的数量。这部分可以做成「库存」类型的指示单。
- **納期 / 状態**（交货期 / 状态）… 预定交货日和明细的状态（已确定、生产中、部分出货）。

### 进行中标签页

排列尚未完成或取消的指示单。点击行会打开[制造指示书](/manual/zh/operations/production/work-order/user)的详情。

- **指示書番号 / 注文明細番号**（指示单编号 / 订单明细编号）… 不与订单挂钩的指示单会带「**在庫向け**」（面向库存）的徽章。
- **種別**（类别）… 是「製造分」（制造）还是「在庫分」（库存）。
- **予定数量 / 承認状態 / 状態**（计划数量 / 审批状态 / 状态）… 计划做几根、审批进行到哪一步、指示单处于哪个阶段（草稿、待审批、已审批、进行中）。

已完成的指示单不会显示在这个画面上。请在[制造指示书](/manual/zh/operations/production/work-order/user)的一览（`PD02`）中确认。

## 从未安排的订单创建指示单

1. 在「**未手配**」（未安排）标签页找到想创建指示单的行。
2. 按行右端的「**指示書作成**」（创建指示单）。
3. 指示单的新建画面会打开。该订单明细已被选中，类别为「**製造分**」（制造），数量已预先填入 **未安排的数量**。
4. 确认内容后保存。之后的步骤请看[制造指示书](/manual/zh/operations/production/work-order/user)的手册。

保存后，「已安排」会相应增加。未安排数量变成 0 的明细会从这个标签页消失。

## 常见问题・遇到困难时

**Q. 原来在列表里的订单明细不见了。**
A. 因为接单数量已全部分配给指示单（未安排变成了 0）。做好的指示单可以在「进行中」标签页或指示单一览（`PD02`）中找到。

**Q. 取消指示单后，订单又回到了未安排标签页。**
A. 这是正确的行为。取消的指示单的数量会从「已安排」中扣除，这部分会回到未安排，可以重新安排。

**Q. 未安排标签页显示「指示書待ちの注文明細はありません」（没有等待指示单的订单明细）。**
A. 意思是现在没有需要安排指示单的订单明细。订单明细在确定后（获得分支编号后）会出现在这里。

**Q. 在哪里能看到已完成的指示单？**
A. 这个画面是工作队列，不显示已完成、已取消的指示单。请在指示单一览（`PD02`）中把状态筛选为「完了」（已完成）。

**Q. 一个订单可以拆成多张指示单吗？**
A. 可以。在「指示書作成」（创建指示单）中减少数量后保存，剩余的数量会作为未安排继续留在这个列表里。

<!-- permissions:start -->
## 所需权限

使用本画面需要 **作业指示书**（`work_order`）权限。

| 想做的事 | 所需权限 |
| --- | --- |
| 打开画面、查看列表与明细 | 作业指示书 — 查看 |
| 新增・变更・删除 | 作业指示书 — 创建 / 更新 / 删除 |

仅查看有「查看」即可。画面中若有新增、变更或删除的操作，则分别需要对应的权限。

权限通过角色授予。若不足，请与管理员联系。

权限的整体说明请参见「[权限与角色](../../../permissions)」。
<!-- permissions:end -->
