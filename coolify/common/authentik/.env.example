# Copy to .env on the server (~/stacks/authentik/.env) — NEVER commit real values.
PG_PASS=<openssl rand -hex 24>
AUTHENTIK_SECRET_KEY=<openssl rand -hex 32>
AUTHENTIK_BOOTSTRAP_PASSWORD=<admin UI password for akadmin>
AUTHENTIK_BOOTSTRAP_TOKEN=<openssl rand -hex 32 — API token for headless setup>
AUTHENTIK_BOOTSTRAP_EMAIL=admin@ckk-tool.co.jp
