# LDAP bind (service account) for Open WebUI AD login. Real values live only in
# .env (gitignored) — never commit credentials here.
LDAP_APP_DN=CN=React_LDAP_User,CN=Users,DC=ckk-tools,DC=loc
LDAP_APP_PASSWORD=your_ldap_bind_password
