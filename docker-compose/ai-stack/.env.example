# ai-stack has no app-specific secrets of its own.
# LDAP/AD credentials for Open WebUI login are centralized in the vpn-ldap stack
# and pulled in via env_file (../vpn-ldap/ldap.env). See docker-compose/vpn-ldap.
