import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export default function PurchaseOutsourceOrdersNewPage() {
  return (
    <PlaceholderPage
      breadcrumbs={["購買", "外注依頼", "新規作成"]}
      title="外注依頼 新規作成"
    />
  );
}
