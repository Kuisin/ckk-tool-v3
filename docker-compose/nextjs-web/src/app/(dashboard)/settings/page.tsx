import { TrialPricingSettingsForm } from "@/components/settings/TrialPricingSettingsForm";

/** システム設定 — 試算 価格ポリシー等. */
export default function SettingsPage() {
  return <TrialPricingSettingsForm />;
}
