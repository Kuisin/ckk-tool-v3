/**
 * mdx-components.tsx — docs ページの MDX コンポーネントマップ。
 * fumadocs の既定（見出しアンカー・コードブロック・Callout 等）をそのまま使う。
 */

import defaultMdxComponents from "fumadocs-ui/mdx";

export const docsMdxComponents = { ...defaultMdxComponents };
