# Cloudflare API token used by acme.sh for the DNS-01 challenge.
# Create at: Cloudflare dashboard > My Profile > API Tokens > Create Token.
# Permissions: Zone > DNS > Edit, scoped to zone kai-lab.net (Zone > Zone > Read).
CLOUDFLARE_DNS_API_TOKEN=
